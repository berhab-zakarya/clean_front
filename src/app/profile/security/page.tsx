import ProfileLayout from "@/app/settings/components/layout/profile-layout"
import SecuritySettings from "@/app/settings/components/security/security-settings"

export default function SecurityPage() {
  return (
    <ProfileLayout>
      <SecuritySettings />
    </ProfileLayout>
  )
}
